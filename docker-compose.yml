version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: code-bounty
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 7888228269shubham
      POSTGRES_DB: codebounty
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
volumes:
  postgres_data: